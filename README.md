# Libtins Windows Seed Project

## Seed project for developing with libtins on Windows.

### Building:

Install `git`, `cmake`, and Visual Studio (version 16 used in the following example).

In windows powershell:
```
mkdir build
cd build
cmake -G"Visual Studio 16 2019" ..
cmake --build .
```
